load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "approval",
    srcs = [
        "manager.go",
        "util.go",
    ],
    importpath = "uml-container/internal/approval",
    visibility = ["//:__subpackages__"],
    deps = ["//internal/audit"],
)

go_test(
    name = "approval_test",
    srcs = ["manager_test.go"],
    embed = [":approval"],
    deps = ["//internal/audit"],
)
