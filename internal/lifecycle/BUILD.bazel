load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "lifecycle",
    srcs = ["autopause.go", "deeppause.go"],
    importpath = "uml-container/internal/lifecycle",
    visibility = ["//:__subpackages__"],
    deps = [
        "//internal/cgroup",
        "//internal/snapshot",
        "//internal/state",
    ],
)

go_test(
    name = "lifecycle_test",
    srcs = [
        "autopause_test.go",
        "deeppause_test.go",
        "epoch_test.go",
    ],
    embed = [":lifecycle"],
    deps = [
        "//internal/state",
    ],
)
