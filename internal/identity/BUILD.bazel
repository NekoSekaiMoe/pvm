load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "identity",
    srcs = [
        "broker.go",
        "persist.go",
    ],
    importpath = "uml-container/internal/identity",
    visibility = ["//:__subpackages__"],
    deps = [
        "//internal/audit",
        "//internal/fsjson",
    ],
)

go_test(
    name = "identity_test",
    srcs = ["broker_test.go"],
    embed = [":identity"],
    deps = ["//internal/audit"],
)
