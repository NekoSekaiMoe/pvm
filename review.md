Inline comments:
In `@internal/vhost/backend.go`:
- Line 42: 移除 internal/vhost/backend.go 中块设备参数对 aio=io_uring 的无条件依赖：在启动存储守护进程前探测
io_uring 是否可用，不支持时回退到 threads，或复用可配置的 AIO 后端设置。同步更新 scripts/test_io_perf.sh
第15-18行，使其验证实际 AIO 后端能力而不只是检查 qemu-storage-daemon 命令存在；两个位置均需覆盖不支持 io_uring
时的安全回退行为。

In `@scripts/test_io_perf.sh`:
- Line 66: Replace the broad qemu-storage-daemon cleanup in the test script with
targeted termination of only the process or vhost socket associated with
perf-test; do not use killall or otherwise affect unrelated host workloads.
- Around line 34-60: 在 loop 挂载命令成功后立即注册清理 trap，确保退出时卸载 mnt。覆盖 tar 解压、init.sh 生成和
chmod 等后续步骤因 set -e 失败的路径，并避免与脚本末尾现有 umount 产生重复或不安全的清理。
- Around line 68-73: Before invoking the `agentpvm run` command in the test
flow, remove the existing `$CONSOLE_LOG` so stale `PERF_TEST_COMPLETED` content
cannot affect the result. Keep the subsequent completion check tied to the
console log generated by the current run, while preserving the existing fallback
output behavior.

